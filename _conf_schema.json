{
  "enable_custom_groups": {
    "description": "启用自定义共享组。关闭时（默认）同一机器人下的所有会话共享全部上下文。",
    "type": "bool",
    "default": false
  },
  "cross_bot_share": {
    "description": "允许跨机器人共享（需同时开启上方开关，且仅限 share_groups 中显式分组的会话）。关闭时不同机器人之间永不共享。",
    "type": "bool",
    "default": false
  },
  "share_groups": {
    "description": "多共享组：键为组名，值为该组的 unified_msg_origin 数组。填写后仅组内的会话互相共享；留空 {} = 共享该机器人的全部会话（默认）。",
    "type": "dict",
    "hint": "添加方法：点修改 → 新键名填组名 → 值类型选 JSON → 值填 [\"qq-bot:FriendMessage:10001\", \"qq-bot:GroupMessage:20002\"]。会话可属于多个组（出现在任一组的列表里即算成员），注入时取所属所有组的成员并集。用内置指令 /sid 可查看当前会话的 umo。未开启 cross_bot_share 时，组内属于其他机器人的条目会被忽略。",
    "default": {}
  },
  "max_messages": {
    "description": "共享池保留的最大消息条数",
    "type": "int",
    "default": 50
  },
  "max_chars": {
    "description": "每轮 LLM 请求注入的字符数上限",
    "type": "int",
    "default": 3000
  },
  "max_message_chars": {
    "description": "单条消息记录时的截断长度",
    "type": "int",
    "default": 200
  },
  "time_window_minutes": {
    "description": "只注入最近 N 分钟内的消息，0 表示不限",
    "type": "int",
    "default": 0
  },
  "include_bot_replies": {
    "description": "是否记录并共享机器人回复",
    "type": "bool",
    "default": true
  },
  "skip_command": {
    "description": "跳过以 / 开头的指令消息",
    "type": "bool",
    "default": true
  }
}
