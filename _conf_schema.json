{
  "custom_groups": {
    "description": "自定义共享组：按组控制哪些会话之间共享上下文",
    "type": "object",
    "items": {
      "enable_custom_groups": {
        "description": "启用自定义共享组",
        "hint": "关闭时（默认）同一机器人下的所有会话共享全部上下文。",
        "type": "bool",
        "default": false
      },
      "out_of_group_mode": {
        "description": "组外会话的处理方式",
        "hint": "未命中任何共享组条目的会话的处理方式（组内只见组内，组外只见组外，互不可见）：isolate = 完全独立（不记录、不注入）；bot = 与本机器人其他组外会话共享；global = 与所有机器人的组外会话共享（不推荐）。",
        "type": "string",
        "options": [
          "isolate",
          "bot",
          "global"
        ],
        "default": "isolate",
        "condition": {
          "enable_custom_groups": true
        }
      },
      "cross_bot_share": {
        "description": "允许跨机器人共享",
        "hint": "仅对 share_groups 中显式分组的会话生效；关闭时不同机器人之间永不共享。",
        "type": "bool",
        "default": false,
        "condition": {
          "enable_custom_groups": true
        }
      },
      "share_groups": {
        "description": "多共享组（JSON：键为组名，值为该组的 umo 数组）",
        "hint": "JSON 格式：{\"组名\": [\"umo1\", \"umo2\"]}。条目支持三种写法：精确会话 umo、*（对任何会话都展开为本机器人内的全部对话）、qq-bot:*（指定机器人的全部会话）。跨机器人双向互通需显式声明对端机器人。示例：{\"汐月\": [\"汐月-QQ:*\", \"汐月-微信:*\"]}。详细用法见 README「自定义共享组」与「跨机器人共享」章节。",
        "type": "text",
        "editor_mode": true,
        "editor_language": "json",
        "default": "{}",
        "condition": {
          "enable_custom_groups": true
        }
      }
    }
  },
  "max_messages": {
    "description": "共享池保留的最大消息条数",
    "type": "int",
    "default": 50
  },
  "max_chars": {
    "description": "每轮 LLM 请求注入的字符数上限",
    "type": "int",
    "default": 3000
  },
  "max_message_chars": {
    "description": "单条消息记录时的截断长度",
    "type": "int",
    "default": 200
  },
  "time_window_minutes": {
    "description": "只注入最近 N 分钟内的消息，0 表示不限",
    "type": "int",
    "default": 0
  },
  "include_bot_replies": {
    "description": "是否记录并共享机器人回复",
    "type": "bool",
    "default": true
  },
  "skip_command": {
    "description": "跳过以 / 开头的指令消息",
    "type": "bool",
    "default": true
  }
}
