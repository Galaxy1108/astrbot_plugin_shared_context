{
  "enable_custom_groups": {
    "description": "启用自定义共享组。关闭时（默认）同一机器人下的所有会话共享全部上下文。",
    "type": "bool",
    "default": false
  },
  "cross_bot_share": {
    "description": "允许跨机器人共享（需同时开启上方开关，且仅限 share_groups 中显式分组的会话）。关闭时不同机器人之间永不共享。",
    "type": "bool",
    "default": false
  },
  "share_groups": {
    "description": "共享白名单：参与共享的会话 unified_msg_origin 列表。填写后仅列表内的会话之间互相共享；留空 = 共享该机器人的全部会话（默认）。",
    "type": "list",
    "items": {
      "type": "string"
    },
    "hint": "每个条目是一个会话的 unified_msg_origin，格式 平台实例ID:消息类型:会话号（私聊 FriendMessage / 群聊 GroupMessage），如 \"汐月-QQ:FriendMessage:3768914943\"。用内置指令 /sid 可查看当前会话的真实值。未开启 cross_bot_share 时，列表里属于其他机器人的条目会被忽略。",
    "default": []
  },
  "max_messages": {
    "description": "共享池保留的最大消息条数",
    "type": "int",
    "default": 50
  },
  "max_chars": {
    "description": "每轮 LLM 请求注入的字符数上限",
    "type": "int",
    "default": 3000
  },
  "max_message_chars": {
    "description": "单条消息记录时的截断长度",
    "type": "int",
    "default": 200
  },
  "time_window_minutes": {
    "description": "只注入最近 N 分钟内的消息，0 表示不限",
    "type": "int",
    "default": 0
  },
  "include_bot_replies": {
    "description": "是否记录并共享机器人回复",
    "type": "bool",
    "default": true
  },
  "skip_command": {
    "description": "跳过以 / 开头的指令消息",
    "type": "bool",
    "default": true
  }
}
